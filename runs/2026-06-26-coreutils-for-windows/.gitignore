# Regenerated by run.sh / fixtures.sh. Kept out of git:
#  - shims/ are ~9MB copies of coreutils.exe (over the 1MB file cap)
#  - the fixtures are byte-exact CRLF/LF files; committing them would let git's
#    autocrlf mangle the very bytes under test. fixtures.sh regenerates + asserts them.
shims/
.work/
probe-output.txt
big.txt
crlf.txt
lf.txt
nums.txt
case.txt
